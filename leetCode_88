class Solution(object):
    def merge(self, nums1, m, nums2, n):
        j=0
        for i in range(len(nums1)):
            if nums1[i]==0 and j<n:
                nums1[i]=nums2[j]
                j+=1
        nums1.sort()
        
        return nums1
