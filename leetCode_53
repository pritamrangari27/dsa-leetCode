class Solution(object):
    def maxSubArray(self, nums):
        maxSum=float("-inf")
        total=0
        for i in range(len(nums)):
            total+=nums[i]
            if total>maxSum:
                maxSum=total
            if total < 0:
                total = 0
        return maxSum
