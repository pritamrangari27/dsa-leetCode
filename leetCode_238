class Solution(object):
    def productExceptSelf(self, nums):
        ans=[]
        n=len(nums)
        for i in range(n):
            product=1
            for j in range(n):
                if i!=j:
                    product*=nums[j]
            ans.append(product)
        return ans
