class Solution(object):
    def containsDuplicate(self, nums):
        mySet=set()
        for i in range(len(nums)):
            if nums[i] in mySet:
                return True
            mySet.add(nums[i])
        return False
        
